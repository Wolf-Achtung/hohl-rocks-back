# .env.example — hohl.rocks backend v2.0
# ------------------------------------------------------------
# Diese Datei dient als VORLAGE. Trage die echten Werte NICHT ein
# und checke KEINE echten Secrets ins Repo ein.
# Auf Railway/Server die Werte im Environment/Variables-UI setzen.
# ------------------------------------------------------------

# === CORS / Herkunfts-Domains (Komma-getrennt) ===
# Beispiel: Produktions-Domains + Netlify Preview + lokaler Dev
CORS_ALLOWLIST=https://hohl.rocks,https://www.hohl.rocks,https://*.netlify.app,http://localhost:8080

# === KI-Provider (optional für /api/run) ===
# Mindestens EIN Provider-Key genügt. Ohne Keys liefert /api/run einen sinnvollen Fallback-Text.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-3-5-sonnet-20241022

OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-3.5-haiku:beta

# === News / Suche (erforderlich für /api/news & /api/daily) ===
TAVILY_API_KEY=
# Optional: Bevorzugte Nachrichten-Domains (DACH). Komma-getrennt.
NEWS_DOMAINS=heise.de,zeit.de,srf.ch,tagesschau.de,the-decoder.de,zdf.de,20min.ch

# === (Optional) Research-Provider-Keys — aktuell nicht aktiv genutzt ===
# PERPLEXITY_API_KEY=
# PERPLEXITY_MODEL=sonar-pro

# === (Optional) Media-Provider (Replicate) — aktuell nicht aktiv genutzt ===
# REPLICATE_API_TOKEN=
# REPLICATE_MODEL_VERSION=black-forest-labs/flux-1.1-pro
# REPLICATE_SDXL_VERSION=
# REPLICATE_MUSICGEN_VERSION=facebook/musicgen
# REPLICATE_LLAVA_VERSION=liuhaotian/llava-13b

# === Server / Limits ===
PORT=8080
NODE_ENV=production
# Beispiel für Rate-Limits (falls später aktiviert):
# RATE_LIMIT_PER_MIN=60
