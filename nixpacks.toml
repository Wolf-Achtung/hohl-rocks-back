[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "npm start"

[health-check]
path = "/health"
timeout = 30
interval = 10
retries = 3
